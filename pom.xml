<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>lunartools</groupId>
	<artifactId>sql-repeatenabler</artifactId>
	<version>2.1.5-snapshot</version>
	<name>SQL-Repeatenabler</name>
	<description>Modifies a SQL-Script (MS flavour) so that it can run more than once, without generating errors like 'table already exists'.</description>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.target>1.8</maven.compiler.target>
		<maven.compiler.source>1.8</maven.compiler.source>
		<lunartools.lunartools.version>1.3.0-SNAPSHOT</lunartools.lunartools.version>
		<org.slf4j.slf4j-api.version>1.7.32</org.slf4j.slf4j-api.version>
		<ch.qos.logback.logback-classic.version>1.2.10</ch.qos.logback.logback-classic.version>
		<ch.qos.logback.logback-core.version>1.2.10</ch.qos.logback.logback-core.version>
		<org.junit.jupiter.junit-jupiter.version>5.10.2</org.junit.jupiter.junit-jupiter.version>
		<maven-surefire-plugin.version>3.2.5</maven-surefire-plugin.version>
	</properties>
	<build>
		<resources>
			<resource>
				<filtering>true</filtering>
				<directory>src/main/resources</directory>
				<includes>
					<include>project.properties</include>
					<include>About_SQL-Repeatabler.html</include>
				</includes>
			</resource>
			<resource>
				<filtering>false</filtering>
				<directory>src/main/resources</directory>
				<excludes>
					<exclude>project.properties</exclude>
					<exclude>About_SQL-Repeatabler.html</exclude>
				</excludes>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<descriptorRefs>
						<descriptorRef>jar-with-dependencies</descriptorRef>
					</descriptorRefs>
					<archive>
						<manifest>
							<mainClass>lunartools.sqlrepeatabler.MainSqlRepeatabler</mainClass>
						</manifest>
					</archive>
					<appendAssemblyId>false</appendAssemblyId>
					<attach>false</attach>
					<finalName>SQL-Repeatabler-${project.version}</finalName>  
				</configuration>
			</plugin>
			<plugin>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${maven-surefire-plugin.version}</version>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<!-- https://mvnrepository.com/artifact/org.slf4j/slf4j-api -->
		<dependency>
		    <groupId>org.slf4j</groupId>
		    <artifactId>slf4j-api</artifactId>
		    <version>${org.slf4j.slf4j-api.version}</version>
		</dependency>
		<!-- https://mvnrepository.com/artifact/ch.qos.logback/logback-classic -->
		<dependency>
		    <groupId>ch.qos.logback</groupId>
		    <artifactId>logback-classic</artifactId>
		    <version>${ch.qos.logback.logback-classic.version}</version>
		</dependency>
		<!-- https://mvnrepository.com/artifact/ch.qos.logback/logback-core -->
		<dependency>
		    <groupId>ch.qos.logback</groupId>
		    <artifactId>logback-core</artifactId>
		    <version>${ch.qos.logback.logback-core.version}</version>
		</dependency>
		<!-- https://github.com/Moon70/LunarTools/packages -->
		<dependency>
			<groupId>lunartools</groupId>
			<artifactId>lunartools</artifactId>
 			<version>${lunartools.lunartools.version}</version>
		</dependency>
		<!-- https://mvnrepository.com/artifact/org.junit.jupiter/junit-jupiter-api -->
		<dependency>
		    <groupId>org.junit.jupiter</groupId>
		    <artifactId>junit-jupiter</artifactId>
		    <version>${org.junit.jupiter.junit-jupiter.version}</version>
		    <scope>test</scope>
		</dependency>
	</dependencies>
</project>